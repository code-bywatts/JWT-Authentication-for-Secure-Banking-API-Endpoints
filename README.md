# JWT-Authentication-for-Secure-Banking-API-Endpoints
Created an Express.js banking API with endpoints for viewing account balance (/balance), depositing money (/deposit), and withdrawing money (/withdraw). Implemented a /login route that accepts a username and password (hardcoded for this exercise) and returns a signed JWT token upon successful login. 
